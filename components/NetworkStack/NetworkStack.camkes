component NetworkStack {
    control;
    provides Network net;

    // Check for this line exactly:
    dataport Buf net_buffer;

    // Hardware interfaces
    dataport Buf virtio_mmio;
    consumes Interrupt hardware_irq;
}
